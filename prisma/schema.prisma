datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model User {
    id String @id
    idType IDType
    name String
    hash String
    role Role
}

enum Role {
    Admin
    User
}

enum IDType {
    NIK
    KTA
}